services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: zentory
      POSTGRES_PASSWORD: zentory
      POSTGRES_DB: zentory
    ports:
      - "5432:5432"
    volumes:
      - zentory-postgres:/var/lib/postgresql/data

volumes:
  zentory-postgres:
